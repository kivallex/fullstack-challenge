<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ../partial/head %>
  </head>
  <body class="container">
    <header>
      <% include ../partial/header %>
    </header>

    <main>
      <div class="jumbotron">
        <h1>Supplyframe Fullstack Challenge</h1>
        <p>-View list of projects from Hackaday in grid mode-</p>
        <p>
          -Display 6 items at a time (meaning by pressing next button, you have
          to see the next 6 items)
        </p>
      </div>
      <div class="row">
        <% if (projects && projects.length) { %> <% var start = 0, end = 6; %>
        <div id="project-grid">
          <% while(start < end) { %>
          <div class="col-sm-4">
            <a href="/detail/<%= projects[start].projectNumber %>">
              <h4>
                <%= projects[start].name %><%= projects[start].projectNumber %>
              </h4>
              <p><%= projects[start].projectDetail %></p>
            </a>
          </div>
          <% start++; %> <% } %>
        </div>
        <% } %>
      </div>
      <script src="/js/projectList.js"></script>
      <% if (start >= 6) { %>
      <button onclick="prev()">Previous</button>
      <% } %> <% if (end < projects.length) { %>
      <button onclick="next()">Next</button>
      <% } %>
    </main>
    <footer>
      <% include ../partial/footer %>
    </footer>
  </body>
</html>
